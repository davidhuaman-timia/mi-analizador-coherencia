version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -m uvicorn aplicacion_MiniLM:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 60
  network:
    port: 8000
