version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -c "import uvicorn; import test_app; uvicorn.run(test_app.app, host='0.0.0.0', port=8000)"
  network:
    port: 8000
  env:
    - name: PYTHONPATH
      value: /app
